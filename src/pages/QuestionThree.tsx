import { QthreeMainContainer } from "../styled/pages/QuestionThree";

const QuestionThree = () => {
  return (
    <QthreeMainContainer>
      <p>Question Three</p>
      <p>Ensure the params Name is be same for both APIs</p>
      <p>
        The maximum number of active concurrent websocket request count is 503.
        it needs to be increase
      </p>
    </QthreeMainContainer>
  );
};

export default QuestionThree;
